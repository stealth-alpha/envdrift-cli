# Demo fixture — a deliberately drifted service.
# Copy this dir somewhere and run: envdrift check --dir demo/app

PORT=3000
LOG_LEVEL=info
FEATURE_FLAGS=on
DATABASE_URL=postgres://localhost:5432/app
